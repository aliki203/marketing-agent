## Context

This is an project that demonstrates how to build a sequential agent pipeline using Google's Agent Development Kit (ADK) with Gradio web interface. The pipeline consists of three specialized agents that work together to generate, review, and refactor Python code based on user requests.

## Project Management

The project is managed using `uv`. 

Use uv add to add new dependencies and uv update to update existing ones.